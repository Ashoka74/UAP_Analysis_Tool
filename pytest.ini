; Run with:  uv run --with pytest pytest
; (pytest is intentionally not added to pyproject.toml — `--with` keeps the
; locked environment untouched.)
[pytest]
testpaths = tests
filterwarnings =
    ignore
addopts = -q
